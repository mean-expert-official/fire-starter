{
  "name": "fire-starter",
  "version": "1.0.0",
  "description": "Starter kit for the fireloop.io platform",
  "scripts": {
    "install": "cd api/ && npm install && cd ../webapp && npm install && cd ..",
    "postinstall": "npm run build",
    "build": "cd webapp/ && npm run build && cd ..",
    "start": "npm run serve",
    "serve": "cd api/ && node server/server.js",
    "test": "npm run install",
    "contributor-add": "all-contributors add",
    "contributor-generate": "all-contributors generate"
  },
  "engines": {
    "node": "^7.0.0",
    "npm": "^4.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "all-contributors-cli": "^4.0.1"
  }
}
