{
	"name": "fireloop-starter",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"setup": "npm run install && npm run serve",
		"preinstall": "npm install -g @angular/cli",
		"install": "cd fireloop/ && npm install && cd ../webapp && npm install && cd ..",
		"postinstall": "npm run build",
		"build": "cd webapp && npm run build && cd ..",
		"serve": "cd fireloop/ && node server/server.js"
	},
	"keywords": [],
	"author": "",
	"license": "ISC"
}
